# /etc/dracut.conf.d/90-nic-link.conf — dracut does NOT copy /etc/systemd/network/*.link by default.
install_items+=" /etc/systemd/network/10-nic-primary.link /etc/systemd/network/10-nic-secondary.link "
# then:  sudo dracut -f /boot/initrd.img-$(uname -r) $(uname -r)
#        sudo lsinitrd /boot/initrd.img-$(uname -r) | grep 10-nic-
